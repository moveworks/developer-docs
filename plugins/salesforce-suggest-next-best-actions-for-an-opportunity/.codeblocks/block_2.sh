{
  "totalCount": 2,
  "results": [
    {
      "id": "{{opportunity_id}}",
      "general": {
        "name": "{{opportunity_name}}",
        "amount": "{{amount}}",
        "stageName": "{{stage_name}}",
        "createdDate": "{{created_date}}"
      },
      "owner": {
        "name": "{{owner_name}}",
        "email": "{{owner_email}}"
      },
      "account": {
        "id": "{{account_id}}",
        "name": "{{account_name}}"
      }
    }
  ]
}
