{
  "totalCount": 2,
  "results": [
    {
      "id": "{{device_id}}",
      "general": {
        "name": "{{device_name}}",
        "platform": "{{device_type}}",
        "assetTag": "{{asset_tag}}",
        "remoteManagement": {
          "managed": "{{active_state}}"
        },
        "lastEnrolledDate": "{{enrollment_date}}"
      },
      "hardware": {
        "model": "{{device_model}}",
        "serialNumber": "{{serial_number}}"
      },
      "userAndLocation": {
        "username": "{{username}}",
        "email": "{{email}}"
      }
    }
  ]
}
